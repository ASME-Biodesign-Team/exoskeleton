#include <stdio.h>
#include "ultrasonic.h"

int main() {

	printf("Hello World!\n");
	// Read in Ultrasonic sensor data and output to screen from this program

	getUltrasonicData();
	/*
	while(1)
	{
		// Error check
		if (y != -1)
			printf("%f\n", y);
	} */

	// Create instance of object
	// Access function in a while loop and catch in var
	// Print to screen
	return 0;
}
